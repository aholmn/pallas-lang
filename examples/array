println([1]);
println([1, 2]);
println([1, 2, 3]);
println([1, 1*2*3/6, "Strings is also supported", true]);
var i = 0;
var arr = [1,3,5,7];

while i < length(arr) do
      println(arr[i]);
      i = i + 1;
end
println([1,3,4][3]);
